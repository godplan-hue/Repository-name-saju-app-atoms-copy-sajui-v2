---
name: project-diet-mbti-fix-plan-pending
description: "2026-08-17 다이어트+MBTI 수정 계획 확정(9개 항목), 코드 수정은 아직 시작 안 함 — 다음 세션/재개 시 이 내용부터 실행"
metadata:
  type: project
  originSessionId: e04af5d5-eb3c-4469-ab5d-37a8c64cef95
  modified: 2026-08-16T22:39:22.010Z
---

## 상태: 계획 확정, 코드 수정 미착수 (에스더님이 다른 작업 먼저 하자고 요청해서 일시 중단)

### 다이어트 앱(jeomun-diet) — 확정 6개 항목
1. **여백/사이즈 복구** (핵심, 아래 원인분석 참고)
2. "이 계절" 섹션 20개(봄/여름/가을/겨울 각 5개)로 복구 — 현재 git에 이미 있는지 재확인 필요
3. "이 계절" 버튼/탭 이름을 계절별 이름으로 수정 (예: "봄" "여름" 등, 현재는 통칭 "이 계절")
4. 첫진입 즉시광고 제거 → "시작하기" 버튼 누를 때 광고 뜨는 방식으로 변경 (다른 앱들과 통일, 토스 심사 리스크+이탈 방지)
5. 광고 딜레이 8초로 통일 (다른 앱들과 동일하게)
6. 설정 탭에 "전화번호 변경" 버튼 작게 추가 (로그아웃 아님, 데이터 삭제 없음 — 기존 입력 흐름 재사용)

### MBTI 앱(jeomun-mbti) — 확정 3개 항목
7. 첫진입 즉시광고 제거 — "시작하기" 버튼 누를 때 광고 있는지 먼저 확인, 없으면 추가/있으면 그대로
8. 광고 딜레이 8초로 통일
9. 크로스프로모 "16개 앱" 소개 리스트가 예전 버전 → 현재 17개 앱 중 본인(MBTI) 제외 16개로 업데이트

### 완료 후 추가 작업
- CLAUDE.md의 "⛔ 광고 흐름(모든 앱 공통, 변경금지) — 앱 열림 → 1.5초 후 첫진입 전면광고" 문구가 이제 낡은 내용(다른 앱들 이미 시작버튼 방식으로 전환됨) → 업데이트 필요

### ⛔ 절대 원칙 (에스더님 두 번 강조)
다이어트 앱은 위 6개 항목 **외에는 글이나 기능 전부 지금 출시된 상태 그대로 유지**. 다른 부분 절대 건드리지 말 것.

---

## 여백 복구 원인 분석 (2026-08-17 확인 완료)

### 처음 시도했다가 폐기한 경로
로컬에 있던 미추적 `dist/`+`jeomun-diet.ait` (8/8 18:34 빌드)를 "잃어버린 78번째 버전"일 것으로 기대하고 포렌식 비교(현재 git HEAD를 새로 빌드해서 바이트 단위 diff)했으나 **완전히 동일한 소스**로 판명 — 이 dist는 라이브 버전이 아니라 그냥 지금 git에 있는 버그버전(77번째)을 빌드한 것뿐이었음. 이 경로는 폐기.

### 실제 원인 확정
git log에서 마지막 2개 커밋이 정확히 "빈공간 제거"를 시도하다 잘림 버그를 만든 지점으로 확인됨:
- `1856d4e` (8/7 02:02): 컨테이너 하단 padding `"20px 16px 120px"` → `"20px 16px 20px"`로 축소
- `0adf9da` (8/7 02:19): CrossPromoMini 뒤 `<div style={{height:20}} />` 스페이서 삭제, `#diet-banner-ad` `minHeight:60→0`으로 변경

### 제안하는 복구 방법 (아직 미실행, 승인 대기 상태였음)
위 3곳만 정확히 원복:
- padding 뒷자리 `20px → 120px`
- `#diet-banner-ad` minHeight `0 → 60`
- CrossPromoMini 뒤 20px 스페이서 div 복원
(CrossPromoMini 자체는 그대로 유지 — 다른 앱 소개 그리드라 그대로 둠)

### 근거 (에스더님이 "여백이 너무 커지는거 아니냐"고 우려해서 확인한 내용)
1. 문제의 2개 커밋 **직전** 로컬 소스가 정확히 이 값이었음
2. 현재 정상 작동 중인 자매앱들도 동일 패턴 사용 확인됨: `jeomun-gamjung`은 `padding:"28px 18px 120px"` + 배너 `minHeight:60`을 그대로 쓰고 있음(라이브, 문제없음). `jeomun-mbti`/`jeomun-momcare`/`jeomun-budget`도 배너 영역 전부 `minHeight:60` 패턴.
3. 100% 픽셀 일치를 육안으로 확인할 방법은 없음(라이브 토스 앱 직접 열어볼 수 없음) — 에스더님이 원하시면 라이브 앱 스크린샷으로 대조 가능. 아니면 위 근거로 바로 진행해도 될 것 같다고 판단.

**다음 세션/재개 시 할 일**: 위 3곳 복구부터 진행 여부 확인받고, 승인되면 다이어트 6개 항목 순서대로 진행 → MBTI 3개 항목 진행 → CLAUDE.md 광고 문구 업데이트.
