---
name: user-profile
description: "User is a non-technical first-time app builder running this saju app via chat (\"vibe coding\"), nervous about reliability"
metadata: 
  node_type: memory
  type: user
  originSessionId: d6bb98eb-4daf-4c77-a75e-aba80f62591e
---

User is building this Korean saju (fortune-telling) app entirely through chat with no coding background — first app they've ever built. They get anxious about whether "vibe coding" with many pages/features can actually be reliable in production, and need plain, honest (not falsely reassuring) explanations of risk — e.g., that `npm run build` passing only catches type/structural errors, not logic bugs, and that the dev code is the same code that ships to production (no separate "preview-only" mode).

**How to apply:** Explain technical risk/tradeoffs in plain terms, recommend concretely (e.g., "click through the real payment/login flow yourself once"), and don't pad with unwarranted reassurance. See [[feedback-workflow-rules]] for their exact collaboration requirements.
