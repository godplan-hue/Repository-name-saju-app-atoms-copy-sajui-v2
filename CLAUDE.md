@AGENTS.md
@FOUNDER.md

> ⛔ **이 문서를 읽지 않고 작업을 시작하지 마라.**
> **이 문서의 규칙은 다른 모든 지시보다 우선한다.**

---

## ⛔ 절대 금지 — 요청한 것만 수정한다

> 이 규칙은 모든 절대 원칙보다 우선한다.

- **사용자가 명시적으로 수정을 요청한 파일·코드 부분만 건드린다.**
- 요청하지 않은 다른 파일, 다른 컴포넌트, 다른 페이지는 **절대로 손대지 않는다.**
- 작업 중 "이것도 고치면 좋겠다"는 생각이 들어도 **하지 않는다.** 사용자에게 먼저 묻는다.
- 새 파일을 만드는 것도 사용자가 명시적으로 요청했을 때만 한다.
- 기존에 잘 동작하는 코드는 요청 없이 절대 수정하지 않는다.

**위반 시**: 사용자가 며칠 동안 복구 작업을 해야 하는 심각한 피해가 발생한다.

---

# 점운 프로젝트 개발 헌법

이 문서는 Claude Code에게 주는 개발 헌법이다.
새 Claude Code 세션이 시작되면 이 문서를 처음부터 끝까지 읽고, 이 원칙을 따라 개발한다.
규칙을 모른 채 코드를 먼저 건드리는 것은 허용되지 않는다.

---

## 프로젝트 목표

대한민국 1위 AI 사주앱을 만드는 것이다.
사주아이의 장점을 벤치마킹하되 그대로 따라하지 않고,
AI 기술과 콘텐츠 설계를 결합하여 더 높은 완성도를 목표로 한다.

---

## 브랜드

- **메인 브랜드명**: 점운
- **메인 앱**: 점운 (jeomun.com)
- 브랜드는 계속 **점운**으로 통일한다.

### 서브브랜드 네이밍 체계 (확정)

**앱 풀네임 (SEO·검색용)**: `점운 + 검색 키워드`
**앱 내부 메뉴·아이콘 (짧게)**: `○운` 형식으로 통일

| 풀네임 (SEO) | 내부 메뉴명 | URL | 상태 |
|---|---|---|---|
| 점운 사주 | 사주 | `/main-v2` | ✅ 운영중 |
| 점운 꿈해몽 | 꿈운 | `/haemong` | ✅ 운영중 |
| 점운 직업·부업 | 직운 | `/jigun` | ✅ 출시 (2026-07-09) |
| 점운 맘케어 | 맘운 | `/momcare` | ✅ 출시 |
| **점운 합격** | **합운** | `/resume` | ✅ 출시 (합격자소서 → 점운 합격) |

> **⛔ 합격자소서 → 점운 합격** 으로 부른다. 코드 내 파일명(`/resume`)은 그대로 유지.
> 향후 추가 예정: 점운 연운(연애), 점운 재운(재물), 점운 궁합, 점운 타로, 점운 작명, 점운 손금

**브랜딩 원칙**:
- 앱스토어·구글 검색: `점운 합격`, `점운 사주`, `점운 꿈해몽` (키워드 포함)
- UI 메뉴·아이콘: `사주`, `꿈운`, `직운`, `맘운`, `합운` (짧게)
- 이 두 레이어를 분리해 SEO와 브랜드 통일성을 동시에 확보한다

---

## 배포 현황 (실제 운영 중)

| 항목 | 내용 |
|------|------|
| 호스팅 | Vercel |
| 도메인 | jeomun.com |
| DB | Firebase Realtime Database |
| 결제 | Toss Payments |
| 파트너 | 서브도메인 방식 화이트라벨 |
| CDN/보안 | Cloudflare (AI 크롤러 차단 + SSL 전체엄격 완료) |

로컬에서 코드를 수정하면 GitHub push 후 **1~2분 뒤** Vercel에 자동 배포된다.
즉시 반영이 아니므로 push 직후 바로 확인하지 말 것.

---

## 기술 스택

- **프레임워크**: Next.js (App Router, app/ 디렉터리 구조)
- **DB**: Firebase RTDB (`lib/firebase.ts`)
- **결제**: Toss Payments
- **스타일링**: 인라인 스타일 (Tailwind 미사용)
- **배포**: Vercel

> ⚠️ `lib/supabase.ts` 파일이 존재하지만 사용하지 않는다.
> Supabase 전환 계획이 있었으나 Firebase RTDB 유지로 결정이 났다.
> 이 파일을 활성화하거나 Supabase로 전환하는 코드를 절대 작성하지 말 것.

---

## 핵심 전략 — 점운은 실시간 AI가 아니다

```
만세력 계산
    ↓
규칙 엔진 (사주 원국, 십성, 신살, 대운, 세운)
    ↓
DB 템플릿 조회
    ↓
답변 출력
```

사용자는 AI와 대화한다고 느끼지만,
실제로는 API를 거의 사용하지 않는 초고속·저비용·안정적인 템플릿 기반 엔진으로 동작한다.

---

## 절대 원칙

1. **API 호출을 최소화한다.** 가능하면 API 없이 구현한다.
2. **템플릿으로 구현 가능한 것은 모두 템플릿으로 구현한다.**
3. 실시간 AI보다 속도·안정성·비용절감을 우선한다.
4. **기존 구조를 함부로 변경하지 않는다.** 항상 기존 코드를 이해한 후 수정한다.
5. 대규모 수정 전에는 반드시 이유를 설명한다.
6. **삭제보다 추가를 우선한다.**

---

## 콘텐츠 전략

AI는 **개발 단계에서만** 사용한다.
Claude를 이용해 사주 원국·십성·신살·대운·세운 등의 콘텐츠를 대량 생성하고,
생성된 콘텐츠는 DB에 저장한다.
서비스에서는 DB만 조회한다.

**목표**
- 질문·답변 DB: 5,000~10,000개 구축
- 답변 템플릿: 수만 개 구축
- API 없이 AI처럼 보이는 서비스 구현

---

## 현재 완성된 기능 (2026년 7월 기준)

### 사용자 앱 (`app/main-v2/`)
- 사주 원국 계산 (만세력, 일주·월주·연주·시주)
- 무료 결과: 오늘의 운세
- 유료 운세 (개별 구매 or 패키지):
  - 재물운, 연애운, 건강운, 성공운
  - 직업/사업운, 배우자운, 건강세부운
  - 부모/형제운, 자녀운
  - 올해운세(월별 포함), 30일 로드맵
- 패키지 상품: 기본분석(990원), 베이직·프리미엄·VIP
- 보관함: 구매 기록 저장 및 재열람 (`app/main-v2/history/`)
- 공유 기능: 카카오톡 등 링크 공유 (`app/main-v2/share/`)
- TTS 읽기 기능: 결과지·파트너결과지·공유·보관함 4곳
  - ↺ 처음부터 듣기 / 이어듣기 / 멈추기
  - 화면꺼짐 대비 및 자동 이어읽기 로직 적용
- 결제 버그 수정 완료 (2026-07-05):
  - 무료 쿠폰 결제 시 Firebase에 ₩990 잘못 저장되던 문제 수정 (`app/main-v2/pay/page.tsx` payFree 함수)
  - 어드민 결제내역에 0원 무료 결제가 표시되던 문제 수정 (`app/api/admin/direct-payments/route.ts` filter 추가)
  - 결제페이지 "인기 운세 9,900원" 섹션 삭제 (잘못된 가격 표시)
  - 메인 "심층 운세 5개 묶음" 카드 클릭 시 번들 모달 대신 "운세 골라담기" 개별 선택 모달로 수정
- 버그 수정 완료 (2026-07-07):
  - 꿈해몽 항상 잠금해제 버그 수정: `payFree`(무료 쿠폰)에서 `haemong_unlock_until` 설정 제거 — 실제 카드 결제(`pay` 함수)만 잠금 해제되도록 수정
  - 대운 결제 후 결과 사라지는 버그 수정: sessionStorage → localStorage+24시간 만료 방식으로 변경, `/payment-complete`(존재하지 않던 URL) 대신 `/main-v2/daewoon?daeunPaid=1` URL로 결제 결과 전달
  - 공유 페이지(`ShareClient.tsx`) 정리: 불필요한 버튼(Q&A, 보관함, 꿈해몽 배너 등) 제거, "AI 사주 990원으로 시작하기" → "나도 무료 사주 받아보기", 카카오공유 문구에서 "990원" 제거
  - 결과지 30% 할인쿠폰에 "1회 신청가" 뱃지 추가
- 버그 수정·기능 추가 완료 (2026-07-07 야간):
  - `/free` 랜딩 쿠폰 → 재물운 표시 버그 3종 수정: `payFree`에 `v2_plan:"select"` 누락 추가(`app/main-v2/pay/page.tsx`), `payFree`에서 `planType:"paid"` 유료 분석 API 재호출해 `allAnalyses` 채우기, `free-lead/route.ts` teaser `\n` 줄바꿈, `free/page.tsx` `whiteSpace:"pre-line"` 추가
  - 결과지 자동 재호출 로직 추가 (`app/main-v2/result/page.tsx`): `tier === "select"` + `allAnalyses` 비어있으면 자동으로 유료 분석 API 재호출 — payFree API 실패해도 결과지에서 재물운이 항상 나오도록 근본 수정
  - 택일 결제 완료 배너 + 버튼 분기 수정 (`app/main-v2/taegil/page.tsx`): 결제 후 "✅ 결제 완료" 배너, 버튼 텍스트 isPaid 조건 분기
  - 결과지 쿠폰 위 재물운 요약 카드 추가 (`app/main-v2/result/page.tsx`): 무료 결과지에서 쿠폰 섹션 바로 위에 오행별 맞춤 3줄 미리보기 카드 표시
  - 택일 유료 해설 대폭 강화 (`app/api/v2/analyze/route.ts`): 12목적 × 4등급 전부 1~2문장 → 4~6문장으로 확장 (시간대·주의사항·팁 포함)
  - `/free` 페이지 ₩3,900 직결 결제 버튼 추가: 쿠폰 없이 바로 결제 가능한 버튼 추가
  - **중요 버그 원인 메모**: `payFree` 내 try-catch가 analyze 실패를 삼켜버려 allAnalyses가 비어 있어도 모름 — 결과지 자동 재호출로 해결됨. 향후 payFree 수정 시 이 구조 유지할 것

### 사주 결과지 2030 콘텐츠 업그레이드 — ✅ 전체 완료 (2026-07-09)

> **수정 파일**: `app/api/v2/analyze/route.ts` 단 하나
> **수정 함수**: `getPackageTemplate()`, `getGenericPaidTemplate()` (=`getPaidTemplate()` 내부에서 호출)
> **⛔ 절대 금지**: 이 함수들 내에서 궁합/2인 파라미터 건드리지 말 것 (VIP 29900원 궁합 기능 망가짐)
> **⛔ 절대 금지**: 결과지 맨 위 노랑/핑크 UI 창·점수 그래프·배너 카드는 result/page.tsx에 있음 — route.ts 수정과 무관, 건드리지 말 것
> **⛔ 절대 금지**: 타로카드·점성술 등 사주와 무관한 요소 절대 삽입 금지

#### 헬퍼 함수 4종 (getPackageTemplate 정의 바로 위에 위치)

```
getDramaCard(name, oh, score, catKey, style)   — K드라마 비유 카드
getOhangTypeCard(oh, catKey)                   — 오행 기질 × MBTI 공명 카드
getRankCard(score, oh, catKey)                 — 오행별 운 순위 (상위 N%)
getPastLifeCard(oh, z)                         — 전생 이야기 (오행×띠 60조합)
```

#### getDramaCard 상세
- 오행(5) × 점수구간(hi≥75/mid≥60/lo<60) × 카테고리(재물/연애/건강/성공/종합) = 75가지 유니크 제목
- 스타일 `premium`: 테두리(━) + 장르+화수+설명 풀버전 → **9900원 패키지** 전용
- 스타일 `viral`: 임팩트 스타일, 스크린샷 공유 최적화 → **990원/3900원 개별** 전용
- 스타일 `standard`: 미사용 (구조상 존재)
- 장르 매핑: 목=성장·청춘극, 화=열혈·로맨스극, 토=가족·생활극, 금=법정·역전극, 수=미스터리·추리극
- 화수 계산: `Math.min(16, Math.max(1, Math.round(score/100*16)))`

#### getOhangTypeCard 상세
- 오행별 MBTI 공명 + 기질 설명 ("사주로 MBTI를 알 수는 없고, 오행 기질이 이런 MBTI와 닮았다"는 프레이밍)
- 목=ENTJ·ENFJ·ENTP / 화=ENFP·ESFP·ESFJ / 토=ISTJ·ISFJ·ESTJ / 금=INTJ·ENTJ·ESTJ / 수=INTP·INFJ·INTJ

#### getRankCard 상세
- 공식: `Math.max(5, Math.round((110 - score) * 0.8 + 5))` → 80점=상위21%, 60점=상위37%
- 점수대별 코멘트: 75+="치고 나갈 타이밍", 60-74="꾸준히 상위권 가능", <60="준비 시간"

#### getPastLifeCard 상세
- 오행(5) × 12띠 = 60조합 전부 고유 전생 직업/신분
- **9900원 패키지 전용** (getPackageTemplate에만 삽입)
- 예: 수오행×쥐띠=점술가, 목오행×호랑이=선발대 장수

#### 각 가격대별 삽입 구성

| 가격대 | 함수 | 카테고리 상단 삽입 내용 |
|--------|------|----------------------|
| 990원/3900원 개별 | `getGenericPaidTemplate` | getDramaCard(viral) + getRankCard + getOhangTypeCard |
| 9900원 패키지 | `getPackageTemplate` | getDramaCard(premium) + getRankCard + getOhangTypeCard + getPastLifeCard |

#### 유명인 비교 (9900원 패키지 전용 Pool A)
- 재물/연애/건강/성공운 각각에 같은 오행의 실제 유명인 예시 삽입
- 변수명: `wealthCelebs`, `loveCelebs`, `successCelebs`, `healthCelebs`
- **반드시 생년 천간으로 오행 검증된 실제 인물만 사용** (팩트 필수, 추정 인물 사용 금지)
- 예: 목=이순신(1545 乙巳)/정주영(1915 乙卯), 화=세종대왕(1397 丁丑)/이해진(1967 丁未), 금=김연아(1990 庚午), 수=손흥민(1992 壬申)/이건희(1942 壬午)

#### 30일 로드맵 점수 기반 인트로 (9900원 패키지 전용)
- 재물/사랑/건강 로드맵 섹션 상단에 점수대별 다른 인트로 삽입
- hi(75+): "✨ ${score}점 — 지금이 가장 강한 에너지입니다"
- mid(60-74): "📈 ${score}점 — 꾸준히 쌓아가는 것이 핵심입니다"
- lo(<60): "🛡️ ${score}점 — 지금은 수비의 시간"

#### getPersonalProfile() 반복 제거 (2026-07-09 추가)
- `getPackageTemplate()`: 이전 세션에서 11곳 반복 제거 완료
- `getGenericPaidTemplate()`: 오늘 6곳 제거 완료 (재물/연애/건강/성공/오늘운세/총운)
  - 각 카테고리 끝부분 "연주 천간으로 보는 뿌리" 섹션 직후에 있던 프로필 블록 제거
  - 주변 글(섹션 헤더, 천간 설명, 마지막 한마디 등) 전부 유지
- 이유: 990원/3900원 여러 카테고리 동시 구매 시 같은 프로필이 반복 노출되던 문제 수정

#### 커밋 이력
- `521c484` — 2030 바이럴 콘텐츠 4종 전 사주 적용 완료 (getDramaCard/getRankCard/getOhangTypeCard/getPastLifeCard)
- `6a6c641` — getGenericPaidTemplate getPersonalProfile 반복 제거

### 파트너 시스템 (`app/partner/`)
서브도메인 방식 화이트라벨 (1개 앱·DB·결제로 여러 파트너 운영)
- 파트너 브랜드 설정, 분석 생성, 결과지, 아카이브, 매출 관리
- 파트너 등급: free·basic·premium·diamond
- 다이아 파트너: 패키지 가격 직접 커스터마이징 가능
- 가입비 미입금 파트너 → 분석 생성 자체 차단

### 관리자 패널 (`app/admin/`)
- 고객 관리, 파트너 관리, 할인코드 관리, 매출 상위 고객

### 대운(大運) — ✅ 출시 완료 (2026-07-06)
- 계산 함수 완성 (대운수·순행역행·60갑자·십이운성)
- 콘텐츠 완성 (십이운성 12단계 × 5섹션, 무료 미리보기/유료 전체해설)
- API 연동 완성, 결제 흐름 완성, 결과지 출시
- 접근 경로: `/main-v2/daewoon` → 결제 → `/main-v2/daewoon/pay` → 결제 후 `/main-v2/daewoon?daeunPaid=1&daeunCount=N&daeunIndices=...`
- localStorage 키: `daeunPaid`, `daeunPaidExpiry`(24시간 만료 타임스탬프), `daeunPaidCount`, `daeunPaidIndices`

### Q&A 시스템 — ✅ 완성
- 11개 카테고리 × ~33문항 × 5오행 = 약 1800개 맞춤 답변 (`lib/qa/`)
- QASection 컴포넌트: `components/QASection.tsx` (카테고리 탭, 검색, 오행 맞춤 답변)
- QAChatWidget 컴포넌트: `components/QAChatWidget.tsx` (복냥이 채팅 형식)
- 전용 페이지: `/main-v2/qa-list` (QASection 단독 페이지)
- 결과지에서 Q&A 버튼/배너 → `/main-v2/qa-list` 이동
- **⛔ QASection을 결과지에 인라인으로 다시 추가하지 말 것 — 이미 qa-list에 있음**

### 고민별 SEO 랜딩 — ✅ 완성 (10개)
- `/love`, `/love-destiny`, `/love-saju` (연애)
- `/marriage` (결혼)
- `/career`, `/career-change` (직업/이직)
- `/health-saju` (건강)
- `/wealth`, `/wealth-saju` (재물)
- `/reunion` (재회)

### 파트너 랜딩 (`/lp/[id]/`) — ✅ 풀 업그레이드 (2026-07-07)
- 파트너별 커스텀 랜딩 페이지 (서브도메인 방식)
- 고객 정보 인테이크 폼 (`LandingForm.tsx`):
  - 1인 폼: 이름/전화번호/생년월일/성별/시간 → `v2_saved_profile` 저장
  - 2인 궁합 폼: 나+상대방 각각 정보 → `v2_saved_profile` + `v2_partner_profile` 저장
- 랜딩 생성기 신기능 (`app/partner/landing-generator/page.tsx`):
  - 색상 테마 6종 (핑크·보라 / 골드·블랙 / 블루·인디고 / 그린·에메 / 다크·신비 / 레드·열정)
  - 히어로 이미지 프리셋 6종 + 커스텀 이미지 URL 직접 입력
  - 1인/2인 폼 유형 선택
  - 프로모션 배너 문구 (입력 시 상단 빨간 배너)
  - 상품 최대 3개 선택 노출 (10개 점운 상품 중 선택)
  - 💎 다이아 전용: 선택 상품별 가격 직접 설정
- Firebase 랜딩 스키마에 새 필드 추가: `heroImageUrl`, `formType`, `promoText`, `packages[]`
- API (`/api/partner/landing` GET): tier 함께 반환 (다이아 여부 판별용)

### 메인 고민 검색창 (FortuneSearch) — ✅ 완성 (2026-07-06)
- 컴포넌트: `app/main-v2/_components/FortuneSearch.tsx`
- 메인 페이지 BannerSlider 바로 아래에 위치
- 키워드 매칭 방식 (API 없음), 12개 카테고리
- 검색 결과 → 각 운세별 결제 페이지로 직접 연결
  - 재물운/연애운/건강운/성공운/사업운/올해운세 → `/main-v2/pay?amount=3900`
  - 결혼궁합/이혼운세 → `/main-v2/pay?amount=990`
  - 재회운/반려동물 → `/main-v2/pay?amount=2900`
  - 대운 → `/main-v2/daewoon/pay`
  - 택일 → `/main-v2/taegil`

### localStorage / sessionStorage 키 정리
- `v2_saved_profile` — 사용자 프로필 JSON (name, birthYear, birthMonth, birthDay, gender, birthHour, isLunar, phone, email)
- `referred_by` — 파트너 ID (파트너 랜딩 → 메인 이동 시 저장)
- `v2_paid_cats` — 결제된 운세 카테고리 배열 (예: `["💰 재물운"]`)
- `daeunPaid`, `daeunPaidExpiry`, `daeunPaidCount`, `daeunPaidIndices` — 대운 결제 상태 (24시간 만료)
- `v2_partner_profile` — 궁합 상대방 프로필 JSON (2인 폼에서만 사용)
- `v2_search_cat` — 검색창에서 선택된 카테고리 키

### 멀티앱 3종 — ✅ 기능화 완료 (2026-07-09)

> 메인 사주 앱(`/main-v2`)에 3개 앱 배너 추가됨 — 비파트너 유저에게만 표시
> **⛔ 결제 연동은 아직 없음** — 버튼 클릭 시 "준비 중" 알림, 실제 Toss 결제는 추후 연동

#### 합격자소서 (`app/resume/`) — ✅ 기능 완성
- **랜딩**: `app/resume/page.tsx` — 합격운 사주 점수 계산기(무료 티저) + 2개 가격 플랜
- **입력 폼**: `app/resume/start/page.tsx` — 이름/생년월일/직무(9개)/기업규모(5개)/회사명/키워드 → POST → 결과지
- **결과지**: `app/resume/result/[id]/page.tsx` — SVG 점수 게이지 + 오행 기질 + 직무 키워드 5개 + 기업별 전략 + 면접 질문 3개 + 택일/사주/꿈해몽 연결 CTA
- **API**: `app/api/resume/analyze/route.ts` (POST/GET) → Firebase `resume_analyses/{id}` 저장
- **오행 계산**: `ganOh = ["목","목","화","화","토","토","금","금","수","수"]`, `idx = (year-4) % 10`
- **점수 공식**: `fieldBonus + sizeBonus + monthBonus + dayVar + ganBonus` → 44~97 클램프
- **콘텐츠**: 9개 직무 × 5개 기업규모 × 5개 오행 = 225가지 맞춤 조합

**가격 플랜 (확정):**
| 플랜 | 가격 | 합격률 표시 | 포함 내용 |
|------|------|-----------|----------|
| 1회 단품 | ₩9,900 | 최고 96점 | 점수+직무전략+키워드3개+면접질문3개+사주총운티저 |
| 5회 풀코스 | ₩29,900 | 최고 98점 | 1회기능×5+기업인재상분석+키워드5개+면접질문5개+오행강점+결과보관+사주총운·직업운·재물운+대운+택일 |

- **가격 전략**: 초반엔 29,900원으로 리뷰 쌓고, 이후 39,900원으로 올리기
- **주의**: 실제 자소서 텍스트를 읽는 게 아님 — 직무+기업규모+생년월일 기반 템플릿 분석
- **⛔ Before/After·피드백·표절탐지·합격사례·경쟁자소서 기능은 없음** — 추가하지 말 것

#### 진로·부업 추천 API (`app/api/career/analyze/`) — ✅ 신규 생성
- POST: 퀴즈 결과 + 추천 부업 목록 → Firebase `career_analyses/{id}` 저장
- GET: id로 결과 조회
- 10가지 부업 상세 템플릿: `content`(SNS크리에이터), `lecture`(강의/코칭), `shop`(스마트스토어), `translate`(번역), `writing`(글쓰기), `consulting`(컨설팅), `design`(디자인/영상), `ai`(AI자동화), `food`(배달/밀키트), `realestate`(부동산)
- 각 부업별: `why`, `howToStart[]`, `platforms[]`, `timeline`, `trap` 포함

#### 맘케어 (`app/momcare/`) — ✅ 서브페이지 3개 추가
- `app/momcare/baby-diary/page.tsx` — 아기 일기
- `app/momcare/time-capsule/page.tsx` — 타임캡슐
- `app/momcare/baby-words/page.tsx` — 아기 말 기록
- 사주·꿈해몽 연결 섹션 추가 (아이 건강운, 태몽 해몽)

#### 파트너 정책 (`app/partner-policy/page.tsx`) — ✅ 업데이트
- "부업으로 시작하는 분들께" 섹션 추가
- 무료 파트너: 결과지 공유 링크 활성화 — 홍보가 많이 되면 이후 유료화 예정

#### 커밋 이력 (이번 세션)
- `62f241c` — 합격자소서 폼/결과지/API 신규, 메인 배너 3개 추가
- `3bf0107` — 결과지 택일·대운 CTA 추가, 메인 앱 배너 연결
- `73750b8` — 맘케어 서브페이지 3개, 사주·꿈해몽 연결
- `cc0ec37` — 파트너 정책 업데이트 (부업 섹션·무료파트너 활성화)
- `537e649` — 합격자소서 2플랜 확정(9,900/29,900), 진로 API 추가

### SEO 현황 (2026-07-06 기준)
- SEO 랜딩페이지: 83개 (`public/sitemap.xml`, 커밋 `742987a`)
- **구글 서치콘솔**: sitemap.xml 제출 완료 ✅, 81개 URL 인덱싱 진행 중
  - 인덱싱 완료까지 2~4주 소요 예정
  - 현재 구글에 노출된 페이지: 3개 (`site:jeomun.com` 기준)
- **네이버 서치어드바이저**: jeomun.com 등록 완료 ✅, sitemap.xml 재제출 완료 (26.07.06 20:38:35) ✅
  - 인증 파일: `public/navered827954b66aa8956e5e07ab70b977f1.html`

---

## 진행 중 / 남은 작업

| 항목 | 상태 |
|------|------|
| 대운 출시 | ✅ 완료 (2026-07-06) |
| 고민 검색창 (FortuneSearch) | ✅ 완료 (2026-07-06) |
| 파트너 랜딩 인테이크 폼 | ✅ 완료 (2026-07-06) |
| 파트너 랜딩 풀 업그레이드 (이미지URL/상품/2인/프로모) | ✅ 완료 (2026-07-07) |
| 유저 추천링크 시스템 | ✅ 완료 (`/api/referral` — 추천인에게 990원 쿠폰 자동 지급) |
| 연도별운세 상품 | ✅ 완료 (`/main-v2/yearly` 존재) |
| 택일(擇日) 기능 | ✅ 완료 (`/taegil` SEO 랜딩 + `/main-v2/taegil` 기능 페이지) |
| 카카오 알림톡 연동 | ⏳ 승인 대기 (심사중, 승인 확인 후 작업) |
| 합격자소서 기능화 | ✅ 완료 (2026-07-09) — `/resume/start`→결과지→사주연결, 9,900/29,900원 2플랜 |
| 맘케어 서브페이지 | ✅ 완료 (2026-07-09) — 아기일기·타임캡슐·아기말 3개 추가 |
| 진로부업 API | ✅ 완료 (2026-07-09) — `/api/career/analyze` 10가지 부업 상세 템플릿 |
| 메인 앱 3개 앱 배너 | ✅ 완료 (2026-07-09) — 맘케어·합격자소서·진로부업 바로가기 |
| 진로앱 결과지 상세화 | ⏳ 미완 — 퀴즈 결과 → `/career-ai/result/[id]` 결과지 페이지 아직 없음 |
| 합격자소서 결제 연동 | ⏳ 미완 — 버튼은 있지만 Toss 결제 미연동 (포트원 완료 후) |
| Cloudflare 보안 설정 | ✅ 완료 (AI 크롤러 차단, SSL 전체엄격) |
| SEO 랜딩 83개 | ✅ 완료 (구글 제출 완료, `/love`·`/career`·`/wealth` 등) |
| 네이버 사이트맵 재제출 | ✅ 완료 (2026-07-06 20:38:35) |
| 포트원 V2 결제 연동 | ⏳ 카카오페이 CID 발급 대기중 (~2주) — CID 받으면 바로 연동 코드 작업 |
| KPN 계약 서류 | ⏳ 개인인감증명서 발급 후 firstpay.co.kr 업로드 + 등기발송 (2주 내 필수) |
| 솔라피 알림톡 템플릿 | ⏳ 채널 연동 완료(@jeomun), 템플릿 등록 필요 — 내용 결정됨(아래 참고) |
| 크몽 AI사주 서비스 등록 | ⏳ 빠른 매출을 위해 바로 진행 권장 |
| 꿈해몽 앱 | ✅ 완료 (`/haemong/` — 카테고리·저장·레이아웃 포함 풀앱) |

---

### 알림톡 템플릿 내용 (결정됨, 등록 필요)
```
[#{파트너명}] 결제가 완료됐어요 💳
결제금액: #{금액}원

사주 분석이 완료됐어요 🔮
아래 링크에서 언제든 다시 확인하세요!
#{링크}
```
- `#{파트너명}` 변수를 넣으면 **템플릿 하나로** 모든 파트너 발송 가능 (점운 직접 고객이면 "점운" 전달)
- 파트너 고객한테는 파트너 상호, 점운 직접 고객한테는 "점운"으로 발송
- 솔라피 채널: @jeomun 연동 완료
- 템플릿 등록 후 카카오 심사 2-3 영업일 → 승인 후 코드 연동

### 광고 전략 확정 (2026-07-07)
- **주력: 구글 애즈** — 점신/사주아이 검색자 직접 타겟, 경쟁사 고객 유입
- **보조: 카카오모먼트** — 관심사 기반, 간접적, 비중 낮춤 (효과대비 비쌈)
- 예산 배분: 구글50% + 네이버30% + 카카오20%
- **실행 조건**: 포트원 결제 연동 완료 후 시작
- **퍼널 필수**: 광고 → 무료사주 → 결과 → 결제 (홈페이지 직접연결 ❌)
- 상세 전략: 메모리 `project_ad_strategy.md` 참조

### 포트원 PG 현황 (2026-07-06)
- KPN 입점심사 진행중 (3영업일 내 연락 예정)
- 카카오페이 카드사 심사 진행중 (~2주) → CID 발급 후 포트원 V2 연동
- **다음 할 일**: 포트원 V2 테스트 연동 → 하나카드 결제창 캡처 → 서브몰심사자료.pptx 작성 → kpnport@kpn.co.kr 회신 (CC: pgs@portone.io)
- KPN 서류: 개인인감증명서 + 등기발송 필수 (2주 내)

---

## 절대 바꾸지 말 것

| 항목 | 이유 |
|------|------|
| Firebase RTDB | 결정된 DB, Supabase 전환 금지 |
| 템플릿 기반 구조 | 실시간 AI API 연동 금지 |
| 파트너 서브도메인 구조 | 운영 중인 파트너 영향 |
| 인라인 스타일 방식 | 프로젝트 전체 일관성 |

---

## 파일 구조 핵심 경로

```
app/
  main-v2/          # 사용자 메인 앱
    result/         # 결과지
    history/        # 보관함
    share/          # 공유 페이지
    payment/        # 결제
  partner/          # 파트너 관리
  admin/            # 관리자 패널
  api/              # API 라우트
lib/
  firebase.ts       # Firebase 연결 (메인 DB)
  constants.ts      # 운세 카테고리, 패키지 정의
  partnerTiers.ts   # 파트너 등급 정의
  supabase.ts       # 사용 안 함 — 건드리지 말 것
```

---

## UI 방향

- 고양이 캐릭터 유지 (브랜드 정체성)
- 귀엽고 친근한 분위기
- 초보자도 이해 가능한 쉬운 문장
- 점집 말투보다 쉽게 설명

---

## 수익화 구조

**대표 상품**: 990원 사주 (기본분석)

**확장 예정**:
- 궁합, 재물운, 직업운, 연애운, 꿈해몽

---

## SEO 전략

앱은 하나만 운영한다. 복사 앱을 여러 개 만들지 않는다.
검색 유입은 웹페이지를 대량 생성하여 확보한다.

타겟 키워드:
오늘의 사주 / 무료 사주 / AI 사주 / 990원 사주 / 고양이 사주 /
궁합 / 재물운 / 직업운 / 만세력 / 신년운세 등
검색 키워드마다 전용 페이지를 생성한다.

---

## Git 원칙

1. **중요 작업 전 반드시 Git 커밋부터 한다.** 작업 도중 문제가 생겨도 되돌아갈 수 있어야 한다.
2. 중요 작업 후 → `git commit` + `git push`
3. 항상 복구 가능하도록 유지한다.
4. 커밋 메시지는 한글로, 무엇을 왜 바꿨는지 명확히 적는다.
5. force push, reset --hard 등 되돌릴 수 없는 명령은 사용자 명시적 승인 없이 절대 실행하지 않는다.

---

## 개발 워크플로우

### 모든 작업에 공통 적용
1. 요청 내용을 파악한다
2. 관련 파일을 먼저 읽는다
3. 기존 코드 구조를 이해한 후 최소한만 수정한다
4. 빌드 오류 없는지 확인한다 (`npx tsc --noEmit`)
5. commit + push
6. 커밋 해시를 보고한다

### 큰 수정(여러 파일 수정 / 구조 변경 / 기능 추가)일 때
> **반드시 이 순서를 지킨다. 임의로 먼저 코드를 건드리지 않는다.**

1. **작업 전 Git 커밋** — 현재 상태를 먼저 저장한다
2. **계획을 먼저 설명한다** — 어떤 파일을 어떻게 바꿀 것인지 사용자에게 보고한다
3. **사용자 승인을 받는다** — "진행해", "해줘" 등 명시적 확인을 받은 후에만 코드를 수정한다
4. 수정 → 빌드 확인 → commit + push → 커밋 해시 보고

예외: 오타 수정, 텍스트 변경, 버튼 추가처럼 영향 범위가 명확히 작은 작업은 바로 진행해도 된다.

---

## 최종 목표

사용자는 AI와 대화한다고 느끼지만,
실제로는 초고속·저비용·안정적인 템플릿 기반 엔진으로 동작하는
**대한민국 최고의 사주 플랫폼**을 만든다.
